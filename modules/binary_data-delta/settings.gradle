rootProject.name = 'binary_data-delta'

if (rootProject.path == ':') {
    include ':modules:binary_data'
    include ':modules:binary_data-array'
}

apply from: '../exbin-auxiliary-modules-settings.gradle'
